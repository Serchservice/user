/// This enum represents different types of pop-up messages.
enum Snackbar {
  /// Represents a success pop-up message.
  success,

  /// Represents an error pop-up message.
  error,

  /// Represents a warning pop-up message.
  warning,

  /// Represents an info pop-up message.
  info
}