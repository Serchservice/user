/// The BiometricAuthState is an enum class with three possible states, `none`, `successful`, and `failed`.
/// This enum represents the different authentication states.
enum BiometricAuthState {
  /// Represents the state when there is no authentication.
  none,

  /// Represents a successful authentication state.
  successful,

  /// Represents a failed authentication state.
  failed,
}